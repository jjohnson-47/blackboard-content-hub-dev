import { describe, it, expect } from 'vitest';

/**
 * Simple test to verify that the testing configuration works
 */
describe('Simple test', () => {
  it('works', () => {
    expect(true).toBe(true);
  });
});